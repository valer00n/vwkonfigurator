<h1>Listing car_trims</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Price</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @car_trims.each do |car_trim| %>
  <tr>
    <td><%= car_trim.name %></td>
    <td><%= car_trim.description %></td>
    <td><%= car_trim.price %></td>
    <td><%= link_to 'Show', car_trim %></td>
    <td><%= link_to 'Edit', edit_car_trim_path(car_trim) %></td>
    <td><%= link_to 'Destroy', car_trim, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Car trim', new_car_trim_path %>
