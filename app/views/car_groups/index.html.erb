<h1>Listing car_groups</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @car_groups.each do |car_group| %>
  <tr>
    <td><%= car_group.name %></td>
    <td><%= link_to 'Show', car_group %></td>
    <td><%= link_to 'Edit', edit_car_group_path(car_group) %></td>
    <td><%= link_to 'Destroy', car_group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Car group', new_car_group_path %>
