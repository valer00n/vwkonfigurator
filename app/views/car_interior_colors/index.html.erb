<h1>Listing car_interior_colors</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Image</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @car_interior_colors.each do |car_interior_color| %>
  <tr>
    <td><%= car_interior_color.name %></td>
    <td><%= car_interior_color.image %></td>
    <td><%= car_interior_color.description %></td>
    <td><%= link_to 'Show', car_interior_color %></td>
    <td><%= link_to 'Edit', edit_car_interior_color_path(car_interior_color) %></td>
    <td><%= link_to 'Destroy', car_interior_color, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Car interior color', new_car_interior_color_path %>
