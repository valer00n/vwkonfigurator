<%= form_for(@car_interior_color) do |f| %>
  <% if @car_interior_color.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@car_interior_color.errors.count, "error") %> prohibited this car_interior_color from being saved:</h2>

      <ul>
      <% @car_interior_color.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :image %><br />
    <%= f.text_field :image %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
