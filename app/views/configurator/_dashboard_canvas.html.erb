<% @carlines.each do |carline| %>
  <div class="grid_4">
    <p class="carline-chooser-cell">
      <%= link_to carline.title, carline %>
    </p>
    <!-- <%= carline.description %> -->
    <!-- <%= carline.price %> -->
  </div>
<% end %>

<div class="grid_16">
  <script type="text/x-handlebars">
    {{ view App.CarlinesView }}
  </script>

  <script type="text/javascript">
    $(document).ready(function() {
      window.car_models = App.store.findAll(App.Carline);
    });
  </script>
</div>