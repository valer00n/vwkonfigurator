<div class="grid_16">
  <h4>Wählen Sie ein Modell aus der Liste aller Fahrzeuge aus.</h4>
</div>
<% @carmodels.each do |carmodel| %>
  <div class="grid_4">
    <p class="carline-chooser-cell">
      <img src="/assets/arrow_orange.gif"><a class="model-chooser-link" href="#<%= carmodel.url %>" token="<%= carmodel.token %>"><%= carmodel.name %></a>
    </p>
  </div>
<% end %>
